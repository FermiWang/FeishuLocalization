#!/bin/sh
set -eu

PROJECT_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHONPATH="$PROJECT_ROOT/src${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONPATH
FEISHU_ARCHIVE_PYTHON=${FEISHU_ARCHIVE_PYTHON:-python3}
exec "$FEISHU_ARCHIVE_PYTHON" -m feishu_archive "$@"
